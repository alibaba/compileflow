#!/usr/bin/env bash
set -eEuo pipefail

script_dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd -P)"
cd "$script_dir/.." || {
    echo "Fail to change work dir!" 1>&2
    exit 1
}

if [ $# != 1 ]; then
    {
        echo "Only 1 argument for version!"
        echo
        echo "usage: $0 <new version>"
    } 1>&2

    exit 1
fi

version="$1"
python3 scripts/check_project_versions.py --syntax-only "$version"

./mvnw versions:set \
    -DgenerateBackupPoms=false \
    -DnewVersion="$version" \
    -B -V --no-transfer-progress

pnpm --dir compileflow-workbench --recursive pkg set "version=$version"
python3 scripts/check_project_versions.py --expected "$version"
