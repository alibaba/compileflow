# CompileFlow Durable PostgreSQL Spring Boot Starter

This Spring Boot starter defines no public Java types of its own. It combines the provider-neutral Durable starter with
the first-party PostgreSQL Store, JDBC support, Flyway migrations, and the PostgreSQL driver. It does not include a process
format; add `compileflow-tbbpm`, `compileflow-bpmn`, or both explicitly.
