# CompileFlow Durable PostgreSQL Spring Boot Starter

This artifact is a dependency-only Spring Boot starter and exposes no Java API.

It combines the provider-neutral Durable starter with the first-party PostgreSQL Store, JDBC, Flyway, and the
PostgreSQL driver.
