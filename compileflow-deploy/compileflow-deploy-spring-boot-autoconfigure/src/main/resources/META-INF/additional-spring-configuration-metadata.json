{
  "properties": [
    {
      "name": "compileflow.deploy.database.provider",
      "description": "Explicit first-party Deploy Store Provider selection when multiple Provider compositions are present."
    },
    {
      "name": "compileflow.deploy.database.migrate",
      "description": "Whether the selected Deploy Store may apply its packaged migration."
    },
    {
      "name": "compileflow.deploy.artifact.mode",
      "defaultValue": "SOURCE"
    },
    {
      "name": "compileflow.deploy.enabled",
      "defaultValue": false
    },
    {
      "name": "compileflow.deploy.runtime-worker-enabled",
      "defaultValue": false
    },
    {
      "name": "compileflow.deploy.topology",
      "defaultValue": "EMBEDDED"
    },
    {
      "name": "compileflow.deploy.routing.aliases",
      "defaultValue": [
        "production"
      ]
    },
    {
      "name": "compileflow.deploy.routing.codes",
      "defaultValue": []
    },
    {
      "name": "compileflow.deploy.routing.namespaces",
      "defaultValue": [
        "default"
      ]
    }
  ],
  "hints": [
    {
      "name": "compileflow.deploy.database.provider",
      "values": [
        {
          "value": "POSTGRESQL",
          "description": "Select the PostgreSQL Deploy Store composition."
        },
        {
          "value": "MYSQL",
          "description": "Select the MySQL Deploy Store composition."
        }
      ]
    },
    {
      "name": "compileflow.deploy.artifact.mode",
      "values": [
        {
          "value": "SOURCE",
          "description": "Resolve immutable artifacts from the selected Deploy Store."
        },
        {
          "value": "PROJECTION_STORE",
          "description": "Publish and resolve immutable artifacts through the deployment projection Store."
        }
      ]
    },
    {
      "name": "compileflow.deploy.topology",
      "values": [
        {
          "value": "EMBEDDED",
          "description": "Project committed routing state into the local engine and recover definitions from the shared database."
        },
        {
          "value": "DISTRIBUTED",
          "description": "Exchange routing state between independently deployable control-plane and runtime processes through a deployment projection store."
        }
      ]
    },
    {
      "name": "compileflow.deploy.reconciliation.mode",
      "values": [
        {
          "value": "DISABLED",
          "description": "Do not schedule derived projection reconciliation."
        },
        {
          "value": "DETECT",
          "description": "Detect and report routing or artifact projection drift without repair."
        },
        {
          "value": "REPAIR",
          "description": "Detect and repair derived routing and artifact projections."
        }
      ]
    }
  ]
}
