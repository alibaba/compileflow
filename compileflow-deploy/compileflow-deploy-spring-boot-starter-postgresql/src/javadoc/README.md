# CompileFlow Deploy PostgreSQL Spring Boot Starter

This starter combines the format-neutral Deploy Spring Boot starter with the PostgreSQL Store provider, JDBC support,
Flyway migrations, and the PostgreSQL driver. It defines no public Java types of its own; add each required
process-format frontend explicitly.
