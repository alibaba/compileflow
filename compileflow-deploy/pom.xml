<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.alibaba.compileflow</groupId>
        <artifactId>compileflow</artifactId>
        <version>2.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>compileflow-deploy</artifactId>
    <packaging>pom</packaging>
    <name>CompileFlow Deployment</name>
    <description>Deployment API, wire protocol, control plane, and node-local runtime.</description>

    <modules>
        <module>compileflow-deploy-api</module>
        <module>compileflow-deploy-protocol</module>
        <module>compileflow-deploy-spi</module>
        <module>compileflow-deploy-testkit</module>
        <module>compileflow-deploy-control-plane</module>
        <module>compileflow-deploy-runtime</module>
        <module>compileflow-deploy-jdbc</module>
        <module>compileflow-deploy-postgresql</module>
        <module>compileflow-deploy-mysql</module>
        <module>compileflow-deploy-spring-boot-autoconfigure</module>
        <module>compileflow-deploy-spring-boot-autoconfigure-postgresql</module>
        <module>compileflow-deploy-spring-boot-autoconfigure-mysql</module>
        <module>compileflow-deploy-spring-boot-starter</module>
        <module>compileflow-deploy-spring-boot-starter-postgresql</module>
        <module>compileflow-deploy-spring-boot-starter-mysql</module>
    </modules>

</project>
